import { ComingSoonPanel } from "@/components/dashboard/ComingSoonPanel";

export default function StagePage() {
  return (
    <ComingSoonPanel
      eyebrow="Next up"
      title="The FIRST module experience is next."
      body="Learn, Do, Coach, and Track tabs for each of the 18 FIRSTS — starting with Core Values Audit."
      color="var(--neon-pink)"
    />
  );
}
